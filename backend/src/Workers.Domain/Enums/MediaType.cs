namespace Workers.Domain.Enums;

/// <summary>
/// Defines the type of media content (image, video, or document).
/// </summary>
public enum MediaType
{
    Image = 1,
    Video = 2,
    Certificate = 3
}
